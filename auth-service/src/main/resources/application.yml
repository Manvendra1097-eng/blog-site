spring:
  application:
    name: auth-service
  datasource:
    url: ${DB_URL:jdbc:mysql://mysql:3306/blog_auth?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=false}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:root}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      "[hibernate.dialect]": org.hibernate.dialect.MySQLDialect

server:
  port: 8081
